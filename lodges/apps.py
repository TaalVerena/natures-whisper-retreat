from django.apps import AppConfig


class LodgesConfig(AppConfig):
    """
    Configuration class for the 'lodges' app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "lodges"
